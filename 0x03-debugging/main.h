#ifndef MAIN_h
#define MAIN.H

#include <stdio.h>

void positive_or_negative(int i_);
int largest_number(int a, int b, int c);
int convert_day(int month, int day);
void print _remaining_days(int month, int day, int year);

#endif
